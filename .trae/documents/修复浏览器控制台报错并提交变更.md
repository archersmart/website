## 报错定位
- 已发现两类控制台报错：
  - Invalid DOM property 警告（class → className），已在 Banner 修复并验证
  - `net::ERR_ABORTED` 来自 Page3 视频资源（外部 mp4），页面初始加载时触发网络错误

## 修复方案
1. Page3 视频懒加载与移动端降级：
   - 在 `src/Home/Page3.jsx` 中：
     - 非移动端渲染 `<Video preload="none" playsInline muted loop ...>`，避免首屏自动拉取 mp4
     - 移动端仅渲染海报图（`<img src={poster}>`），不加载视频，规避移动网络错误
     - 添加 `onError` 捕获并静默处理，保证用户体验不受影响
2. 复查其它 Invalid DOM property：
   - 再次全局扫描是否存在 `class` 等错误属性，确保警告清零

## 提交
- 使用规范的提交信息：
  - `fix(page3): lazy-load video and mobile fallback to poster`
  - `fix(banner): replace class with className to remove console warning`

## 验证
- 启动开发服务并打开首页、Page3 区域
- 检查浏览器控制台，确认不再出现资源加载错误与属性警告

确认后我将按照以上方案修改代码、验证并执行 commit。